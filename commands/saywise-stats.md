---
description: Measure local Claude Code usage and show the aggregate stats.
---

Load the `saywise-stats` skill, then follow it exactly: run its bundled scan script and show the user the summary table and the JSON.

Aggregate numbers only — never prompts, project names, file paths, or conversation content. Nothing is uploaded anywhere.
